# PRISM — 镭射折叠实验室

独立的静态网站，入口为 `index.html`。原有 `MacBook-Duo` 目录不受影响。

## 本地运行

在本目录运行 `python3 -m http.server 4173 --bind 0.0.0.0`，打开 http://localhost:4173 。手机与电脑在同一 Wi-Fi 时，使用电脑的局域网 IP 和端口 4173 访问；不要在手机上使用 localhost。

## 玩法

- 左右拖动模型区域调整 0–180° 折叠；点击模型或按钮开合。
- 滑杆精确控制角度，播放按钮循环演示开合。
- 光谱、极光、银铬三种材质；折叠角度与指针位置驱动反光，两侧有独立色相差。
- 转动追光模式：拖动改变观察方向；移动端为手指控制，未接入陀螺仪。
- 卡片悬浮模式：卡面从屏幕升起，可与追光模式叠加。
- 中英文界面、明暗主题、一键重置。模型区域支持左右方向键、Home、End、空格。

## 实现与边界

使用 CSS 3D 双面模型、独立铰链变换、程序化镭射图案和原生 JavaScript。无需打包或第三方运行库；Google Fonts 不可用时使用系统字体。这里是原创概念手机，并非 Apple 官方机型模型，也不包含 Blender 或 GLB 文件。

交互参考：https://bonxn.github.io/dood-iphone-duo/ 。图案、网页布局和代码为本项目独立实现。尚未发布到公网。
